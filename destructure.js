const person = {
    name: 'Tom',
    age:24,
    job: 'catch mouse',
    gfName: 'pakhi',
    address: 'cn network',
    phone: '015124121',
    friends: ['kudus', 'akkas', 'potol'],
}

const { phone, gfName, address } = person;

console.log(phone, gfName, address);
console.log(phone, gfName, address);
console.log(phone, gfName, address);
console.log(phone, gfName, address);